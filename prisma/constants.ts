export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN'
  }
  
   export enum CollaboratorTypeEnum {
    RESPONSIBLE = 'RESPONSIBLE',
    ASSISTANT = 'ASSISTANT',
    ON_FIELD = 'ON_FIELD'
  }
  
  export enum ProjectTypeEnum {
    REPORTES_CURVAS = 'REPORTES_CURVAS',
    PLANO_CATASTRO = 'PLANO_CATASTRO'
  }
  
 export  enum Status {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE'
  }